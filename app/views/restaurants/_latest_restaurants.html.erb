<% @restaurants.each do |restaurant| %>

    <div class="col-4">
    <%= link_to restaurant_path(restaurant.id) do %>
    <%= image_tag restaurant.photo_url(:thumbnail).to_s %>
    <% end %>
    <h3 class="h5 mt-3"><%= restaurant.name %></h3>
    <p><%= restaurant.address %></p>
    <p><%= restaurant.cuisine %></p>
    </div>

<% end %>