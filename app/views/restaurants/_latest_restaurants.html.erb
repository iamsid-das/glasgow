<% @restaurants.each do |restaurant| %>

    <div class="col-4">
    <%= link_to restaurant_path(restaurant) %>
    <h3 class="h5 mt-3"><%=link_to restaurant_path(restaurant), restaurant.name %></h3>
    <p><%= restaurant.address %></p>
    </div>

<% end %>